export interface ICustomersIntl {
  metadata: {
    title: string;
  };
  heading: {
    title: string;
    button: string;
  };
  table: {
    columns: {
      name: string;
      status: string;
      email: string;
      birth: string;
      phone: string;
    };
  };
}
